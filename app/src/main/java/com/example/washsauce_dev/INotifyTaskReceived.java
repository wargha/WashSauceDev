package com.example.washsauce_dev;

//Interface created with the purpose of notifying when a task is received by the USER.
public interface INotifyTaskReceived {
    void notifyTaskResult(Task task);
}
